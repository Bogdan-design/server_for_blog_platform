export const serviceLogin = {
    async createAuthToken (loginOrEmail : string, password: string){
        const token = await serviceLogin.createAuthToken(loginOrEmail, password);
        return {
            
        }
    }
}